<!DOCTYPE html>
<html>
  <head>
    <title>PhotoSharing</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="wlink_textidth=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    
  </head>

  <body>
    <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <h5 class="my-0 mr-md-auto font-weight-normal">Photo Sharing</h5>
      <% if logged_in? %>
        <%= link_to  "New post", new_post_path, class:"btn btn-outline-primary mr-3" %>
        <%= link_to  "All post", posts_path, class:"btn btn-outline-primary mr-3" %>
        <%= link_to  "Favorite", favorites_path, class:"btn btn-outline-primary mr-3" %>
        <%= link_to  "Profile", edit_post_path(current_user.id), class:"btn btn-outline-primary mr-3" %>
        <%= link_to  "Logout", session_path(current_user.id), method: :delete, data:{confirm: "Are you sure ?"}, class:"btn btn-outline-primary mr-3"  %>
      <% else %>
        <%= link_to  "Sign in", new_session_path, class:"btn btn-outline-primary mr-3" %>
        <%= link_to  "Sign up", new_user_path, class:"btn btn-outline-primary" %>
      <% end %>
    </div>
    <div class="main">
      <%= yield %>
    </div>
    <footer class="pt-4 border-top">
          <div class="col-12 col-md">
            <span>DiveIntoCode | All right reserved &copy; 2021</span>
          </div>
      </footer>
  </body>
</html>
